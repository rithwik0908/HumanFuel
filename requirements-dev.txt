# Development install: the package plus its dev and reports extras (kept in sync with pyproject.toml).
# Runtime deps (pandas, numpy, matplotlib, pyyaml, openpyxl) and optional plotly come from the package.
-e .[dev,reports]
